# Copia este archivo como .env y completa con tus credenciales
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=rol_turno

JWT_SECRET=pon-aqui-una-clave-larga-y-secreta

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

PORT=3001

# Correo (OPCIONAL). Si lo dejas vacío, las notificaciones solo serán in-app (campanita).
# Ejemplo con Gmail: host smtp.gmail.com, puerto 587, y una "contraseña de aplicación".
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Rol de Turno HMEP <no-reply@hmep.hn>
